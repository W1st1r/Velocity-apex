(function(){
  'use strict';
  const $=id=>document.getElementById(id),root=$('friends'),list=$('friendsList'),message=$('friendsMessage'),count=$('friendsCount'),menuStatus=$('friendsMenuStatus');
  if(!root)return;
  let friends=[],busy=false;
  async function api(path,opts={}){const r=await fetch(path,{credentials:'same-origin',headers:{'content-type':'application/json',...(opts.headers||{})},...opts});let d={};try{d=await r.json();}catch{}if(!r.ok){const e=new Error(d.error||'SERVER_ERROR');e.code=d.error||'SERVER_ERROR';throw e;}return d;}
  const statusText=f=>f.online?'ОНЛАЙН':(f.lastSeenAt?('БЫЛ(А) '+new Date(f.lastSeenAt).toLocaleString('ru-RU',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'})):'ОФЛАЙН');
  function render(){list.replaceChildren();const online=friends.filter(f=>f.online).length;count.textContent=`${friends.length} друзей · ${online} онлайн`;menuStatus.textContent=online?`${online} ОНЛАЙН`:String(friends.length||'—');
    if(!friends.length){const e=document.createElement('div');e.className='friends-empty';e.innerHTML='<strong>СПИСОК ПОКА ПУСТ</strong><span>Добавьте игрока по его @username.</span>';list.appendChild(e);return;}
    for(const f of [...friends].sort((a,b)=>Number(b.online)-Number(a.online)||a.username.localeCompare(b.username))){const row=document.createElement('div');row.className='friend-row';const avatar=document.createElement('span');avatar.className='friend-avatar';avatar.textContent=(f.displayName||f.username||'VA').slice(0,2).toUpperCase();const copy=document.createElement('div');copy.className='friend-copy';const n=document.createElement('strong');n.textContent=f.displayName;const u=document.createElement('small');u.textContent='@'+f.username;copy.append(n,u);const s=document.createElement('span');s.className='friend-status '+(f.online?'online':'offline');s.textContent=statusText(f);const remove=document.createElement('button');remove.type='button';remove.className='friend-remove';remove.textContent='УДАЛИТЬ';remove.addEventListener('click',()=>removeFriend(f.username));row.append(avatar,copy,s,remove);list.appendChild(row);}}
  async function load(silent=false){try{const d=await api('/api/friends');friends=d.friends||[];render();if(!silent)message.textContent='';}catch(e){if(!silent){message.textContent=e.code==='AUTH_REQUIRED'?'Войдите в аккаунт, чтобы использовать друзей.':'Не удалось загрузить друзей.';message.classList.add('bad');}}}
  async function add(){if(busy)return;const input=$('friendUsername'),username=input.value.trim().replace(/^@/,'').toLowerCase();if(!username){message.textContent='Введите ник игрока.';message.className='account-message bad';return;}busy=true;$('friendAddBtn').disabled=true;try{await api('/api/friends/add',{method:'POST',body:JSON.stringify({username})});input.value='';message.textContent='Друг добавлен.';message.className='account-message';await load(true);}catch(e){const map={USER_NOT_FOUND:'Игрок не найден.',ALREADY_FRIENDS:'Этот игрок уже в друзьях.',CANNOT_ADD_SELF:'Нельзя добавить самого себя.',AUTH_REQUIRED:'Нужен вход в аккаунт.'};message.textContent=map[e.code]||'Не удалось добавить друга.';message.className='account-message bad';}finally{busy=false;$('friendAddBtn').disabled=false;}}
  async function removeFriend(username){try{await api('/api/friends/remove',{method:'POST',body:JSON.stringify({username})});friends=friends.filter(f=>f.username!==username);render();}catch{message.textContent='Не удалось удалить друга.';message.className='account-message bad';}}
  function open(){root.classList.remove('hidden');root.setAttribute('aria-hidden','false');$('menu')?.classList.add('hidden');load();}
  function close(){root.classList.add('hidden');root.setAttribute('aria-hidden','true');$('menu')?.classList.remove('hidden');}
  $('friendsBtn')?.addEventListener('click',open);$('friendsCloseBtn')?.addEventListener('click',close);$('friendAddBtn')?.addEventListener('click',add);$('friendUsername')?.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();add();}});root.addEventListener('click',e=>{if(e.target===root)close();});
  async function presence(){try{await api('/api/friends/presence',{method:'POST',body:'{}'});if(!root.classList.contains('hidden'))await load(true);}catch{}}
  setInterval(presence,45000);setTimeout(()=>{load(true);presence();},1500);document.addEventListener('visibilitychange',()=>{if(!document.hidden)presence();});
  window.VelocityFriends={open,close,load,get list(){return friends;}};
})();
